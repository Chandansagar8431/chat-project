import React from "react";
import { Box } from "@mui/material";

const Postwidget = ({
  postid,
  postUserid,
  name,
  location,
  occupation,
  likes,
  comments,
  description,
  picturepath,
  userpicturepath,
}) => {
  return <Box>{}</Box>;
};

export default Postwidget;
